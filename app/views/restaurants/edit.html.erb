<h1>Editing restaurant</h1>

<div>
  <%= link_to "Show this restaurant", @restaurant %> |
  
</div>
<%= form_for(@restaurant) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :status %>
  <%= f.select :status, ['open', 'closed'] %>

  <%= f.submit %>
<% end %>
